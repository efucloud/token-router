# Project notes

- React 19 + Ant Design 6 + Ant Design Pro 6 + Umi Max 4 + TypeScript strict mode.
- `src/services` 由 `backend/generate` 生成，不要手工修改。
- 前端通过 OIDC 登录，包含个人对话、用量看板、API Token 与系统管理页面。
- 使用 `npm` 和 `package-lock.json`。
- 提交前运行 `npm run lint` 和 `npm run build`。
